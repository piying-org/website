v.pipe(
  v.object({
    support: v.pipe(
      v.optional(v.picklist(["Angular", "Vue", "React", "Svelte", "Solid"),
      v.title($localize`当前支持`),
    ),
    rating: v.pipe(
      v.optional(v.number()),
      setComponent("rating"),
      v.title($localize`评价`),
    ),
    usePiying: v.pipe(v.boolean(), v.title($localize`使用皮影`)),
  }),
  setComponent("fieldset"),
);
